// function txt_to_json(txtfile: Blob) {
// 	var reader = new FileReader();
// 	reader.readAsText(txtfile);
// 	var text = reader.result as string,
// 	tempstr = "";
// 	for (let i=0; i<text.length; i++) {
// 		let ch = text[i];
// 		if (ch == '\r') continue;
// 		else if (ch == '\n')
// 	}
// }